(function() {    const button = document.createElement("button");    button.innerText = "HACK";    button.id = "mainButton";    button.addEventListener("click", () => {        const iframe = document.createElement("iframe");        iframe.src = "https://HACK.thatoneweirddev.repl.co";        iframe.style.position = "absolute";        iframe.style.bottom = "0";        iframe.style.right = "0";        iframe.style.width = "500px";        iframe.style.height = "400px";        document.body.appendChild(iframe);        document.designMode = "on";        alert("Created by Ari");        button.style.display = "none"    });    document.body.appendChild(button);    button.style.border = "2px solid black";    button.addEventListener("mouseover", () => {        button.style.filter = "blur(.5px)"    });    button.addEventListener("mouseout", () => {        button.style.filter = "none"    });    window.addEventListener('message', function(event) {        if (event.data === 'closeIframe') {            var iframes = document.getElementsByTagName('iframe');            for (var i = 0; i < iframes.length; i++) {                if (iframes[i].contentWindow === event.source) {                    iframes[i].parentNode.removeChild(iframes[i]);                    break                }            }        }    });    window.addEventListener('message', function(event) {        if (event.data === 'upsideDownImages') {            var images = document.getElementsByTagName('img');            for (var i = 0; i < images.length; i++) {                images[i].style.transform = 'rotate(180deg)'            }        }    })})();
